{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "markdown-to-jira",
  "title": "Markdown to Jira",
  "description": "Format a Markdown text into Jira markup.",
  "icon": "command-icon.png",
  "author": "theo_brlle",
  "owner": "theo-brlle",
  "categories": [
    "Documentation"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Markdown to Jira",
      "subtitle": "Format Markdown text to Jira markup",
      "description": "Format a Markdown text into Jira markup.",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.52.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "1.0.5",
    "@types/node": "18.8.3",
    "@types/react": "18.0.9",
    "eslint": "^7.32.0",
    "prettier": "^2.5.1",
    "typescript": "^4.4.3"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}